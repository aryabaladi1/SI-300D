<Window x:Class="SI_300D.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:SI_300D.Converters"
        Title="SI-300D"
        Height="600"
        Width="1000"
        MinHeight="500"
        MinWidth="800">
    <Window.Resources>

        <converters:BooleanToVisibilityConverter
        x:Key="BooleanToVisibilityConverter"/>

        <converters:InverseBooleanToVisibilityConverter
        x:Key="InverseBooleanToVisibilityConverter"/>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="25"/>
            <Setter Property="Background" Value="#F5F5F5"/>
        </Style>
    </Window.Resources>

    <Grid Margin="30">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0">
            <StackPanel>
                <TextBlock
                    Text="{Binding ApplicationName}"
                    FontSize="30"
                    FontWeight="SemiBold"/>

                <TextBlock
                    Text="Network Monitor"
                    FontSize="14"
                    Opacity="0.6"
                    Margin="0,4,0,0"/>
            </StackPanel>

            <StackPanel
                HorizontalAlignment="Right"
                VerticalAlignment="Center">

                <TextBlock
                    Text="● Monitoring"
                    FontSize="14"
                    Visibility="{Binding IsMonitoring, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                <TextBlock
                    Text="○ Stopped"
                    FontSize="14"
                    Visibility="{Binding IsMonitoring, Converter={StaticResource InverseBooleanToVisibilityConverter}}"/>

            </StackPanel>
        </Grid>

        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="0,30">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Traffic -->
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border
                    Grid.Column="0"
                    Style="{StaticResource CardStyle}">

                    <StackPanel>

                        <TextBlock
                            Text="↓ DOWNLOAD"
                            FontSize="13"
                            FontWeight="SemiBold"
                            Opacity="0.6"/>

                        <TextBlock
                            Text="{Binding DownloadBytesPerSecond, StringFormat={}{0:N0} B/s}"
                            FontSize="32"
                            FontWeight="SemiBold"
                            Margin="0,8,0,0"/>

                        <TextBlock
                            Text="Current speed"
                            FontSize="12"
                            Opacity="0.5"
                            Margin="0,4,0,0"/>

                    </StackPanel>

                </Border>

                <Border
                    Grid.Column="2"
                    Style="{StaticResource CardStyle}">

                    <StackPanel>

                        <TextBlock
                            Text="↑ UPLOAD"
                            FontSize="13"
                            FontWeight="SemiBold"
                            Opacity="0.6"/>

                        <TextBlock
                            Text="{Binding UploadBytesPerSecond, StringFormat={}{0:N0} B/s}"
                            FontSize="32"
                            FontWeight="SemiBold"
                            Margin="0,8,0,0"/>

                        <TextBlock
                            Text="Current speed"
                            FontSize="12"
                            Opacity="0.5"
                            Margin="0,4,0,0"/>

                    </StackPanel>

                </Border>
            </Grid>

            <!-- Interface -->
            <Border
                Grid.Row="1"
                Padding="25"
                Margin="0,20,0,0">

                <StackPanel>

                    <TextBlock
                        Text="NETWORK INTERFACE"
                        FontSize="13"
                        Opacity="0.6"/>

                    <TextBlock
                        Text="{Binding InterfaceName}"
                        FontSize="22"
                        FontWeight="SemiBold"
                        Margin="0,8,0,0"/>

                    <TextBlock
                        Text="{Binding InterfaceType}"
                        FontSize="14"
                        Opacity="0.6"
                        Margin="0,4,0,0"/>

                    <TextBlock
                        Text="{Binding InterfaceStatus}"
                        FontSize="14"
                        Margin="0,12,0,0"/>

                </StackPanel>

            </Border>

        </Grid>

        <!-- Controls -->
        <StackPanel
            Grid.Row="2"
            Orientation="Horizontal"
            HorizontalAlignment="Right">

            <Button
                Content="Start Monitoring"
                Click="StartMonitoring_Click"
                Padding="20,10"
                Margin="0,0,10,0"/>

            <Button
                Content="Stop Monitoring"
                Click="StopMonitoring_Click"
                Padding="20,10"/>

        </StackPanel>

    </Grid>

</Window>